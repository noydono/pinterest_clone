{% extends 'base.html.twig' %} 
{% block title 'Panterest' %} {% block body %}
<h1>{{ pluralize(pins | length, "Pin") }}</h1>

{% for pin in pins %}
<h2>
  <a href="{{ path('app_pins_show', {id:pin.id}) }}"> {{ pin.title }}</a>
</h2>
<p>{{ pin.content }}</p>
<p>{{ pin.id }}</p>
{% endfor %} {% endblock %}
